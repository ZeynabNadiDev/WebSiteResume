@model Resume.Domain.ViewModels.Reservation.CreateOrUpdateReservationViewModel

@{
    bool isCreate = Model.Id == 0 ? true : false;
}


<form id="ReservationForm" asp-area="Admin" asp-controller="Reservation" asp-action="SubmitReservationFormModal"
      data-ajax="true" data-ajax-begin="StartLoading()" data-ajax-success="ReservationFormSubmited">

    <div class="modal-header">
        <h5 class="modal-title">@(isCreate ? "ایجاد" : "ویرایش")</h5>
        <button type="button" class="close" data-dismiss="modal">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>


    <div class="modal-body">

        <div class="row">
            <div class="col-12">

                @if (!isCreate)
                {
                    <input type="hidden" asp-for="Id" />
                }

                <div class="form-group">
                    <label asp-for="ReservationDate">تاریخ نوبت</label>
                    <div class="controls">
                        <input type="text" DatePicker="1" asp-for="ReservationDate" class="form-control">
                    </div>
                    <span asp-validation-for="ReservationDate" class="text-danger"></span>
                </div>

                <div>
                    <button type="submit" class="btn btn-bold btn-pure float-right @(isCreate ? "btn-success" : "btn-warning")">@(isCreate ? "ایجاد" : "ویرایش")</button>
                </div>

            </div>
        </div>

    </div>

</form>